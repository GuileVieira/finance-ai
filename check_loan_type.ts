import { db } from './lib/db/drizzle'; import { categories } from './lib/db/schema'; import { initializeDatabase } from './lib/db/init-db'; import { eq, ilike } from 'drizzle-orm'; async function main() { await initializeDatabase(); const cats = await db.select().from(categories).where(ilike(categories.name, 'EMPRÉSTIMOS (+)')); console.log('Category EMPRÉSTIMOS (+):', cats[0]); } main().catch(console.error).then(() => process.exit(0));
